# 稽查：資料會不會跑到不該去的地方

這個 app 的每一份部署只有一個擁有者，而「你的資料只有你看得到」是它唯一真正
的安全承諾。這份文件說明那個承諾是**怎麼被反覆驗證的**，以及你要怎麼自己跑一次。

一次性的稽查只能證明「那一天沒事」。所以這裡有三層，愈上面愈頻繁、愈自動。

---

## 第一層：每次 push（自動，免費，不需要 AI）

`.github/workflows/ci.yml` 的 backend job 裡有一個關卡：

```
python scripts/audit.py
```

它跟其他測試不一樣的地方在於**它不知道這個 app 有什麼**——端點是從跑起來的 app 上
讀出來的，資料表是從模型登記簿讀出來的。它知道的只有「什麼事絕對不可以發生」。

六種方法，刻意互相獨立（躲得過一種的洞會在另一種現形）：

| 方法 | 問的問題 |
| --- | --- |
| 一、清冊 | 每一個操作都要有帳號閘門。沒有的必須在 `PUBLIC_ON_PURPOSE` 名單上**並附理由** |
| 二、誘餌（匿名） | 沒有登入的人打遍所有操作，回應裡不准出現任何誘餌 |
| 三、誘餌（別的帳號） | 第二個帳號打遍所有操作，包含把擁有者的資源 id 代進網址 |
| 四、覆蓋 | 每一張帶 `user_id` 的表都要真的被植入過誘餌，植不進去的會被列出來 |
| 五、沙箱 | 策略沙箱的逃逸樣本庫 |
| 六、倉庫 | 工作區裡不准出現金鑰形狀的字串、本機路徑、私鑰 |

**誘餌（canary）是什麼**：在 app 被 import 之前，每一個密鑰都被換成一個世界上
不存在第二份的字串，擁有者的每一列資料也帶著一個。最後的問題就變成純粹的字串比對：
那些字串有沒有跑出來。這比「回應看起來像不像有問題」難騙得多。

自己跑：

```bash
cd backend
venv/Scripts/python.exe scripts/audit.py          # 全部
venv/Scripts/python.exe scripts/audit.py --fast   # 略過倉庫全文掃描
```

**新增一個公開端點會讓 CI 紅燈**，直到有人把它寫進 `PUBLIC_ON_PURPOSE` 並說出理由。
這是刻意的：那份名單就是「這些公開是想清楚的」這句話的證據。

---

## 第二層：每週一次（自動）＋ 稽查線上那一份

`.github/workflows/audit.yml`，每週一跑，也可以自己按（Actions → Audit → Run workflow）。

它多做兩件第一層做不到的事：

1. **沒有人推東西的那幾天也照樣問一次。** 相依套件會在你沒動程式的時候被公告漏洞。
2. **稽查真的部署出去的那一份**：

```bash
venv/Scripts/python.exe scripts/audit.py --url https://你的後端/healthz
```

工作目錄裡是什麼，跟公開網路上跑著什麼，是兩件可以不一樣的事。這一項會：

- **拿這份程式碼宣告的端點清單去打線上**：宣告要登入的，線上就不可以在沒有登入時
  回 200。它測的是那扇門本身，不是門上貼的說明——所以線上跑著舊版、或平台上有人
  設了某個環境變數，都會在這裡現形。
- 確認 `/openapi.json`、`/docs`、`/redoc` 在線上是**打不開的**。裡面沒有使用者資料，
  所以那不是外洩，但它是一份完整的端點地圖，交給任何知道後端網址的人——而那個網址
  在前端每一個請求裡。`ENABLE_API_DOCS` 預設就是關的（見 `app/main.py::docs_urls`）。
- 確認 `/healthz` 沒有把你在看的代號名稱端出去。
- 確認這個部署已經有擁有者（沒有的話，第一個註冊的人就會變成擁有者——那是一個要
  立刻處理的狀況）。

**這一項是唯讀的**：只有 GET，不會在線上資料庫寫任何東西。要真的去試註冊那扇門，
要自己加 `--live-write-probe`，而且門開著的話它會建立一個帳號——所以預設不做。

---

## 第三層：深掃（需要時觸發，用 AI）

規則抓得到「寫得出規則」的東西。上一次找到的六個真漏洞裡，有幾個規則寫不出來——
例如策略沙箱那個「六行程式碼、沒有一個 dunder」的逃逸。那種需要有人（或 AI）
去想「如果我要偷，我會怎麼走」。

**怎麼觸發**：在這個 repo 開一個 session，說：

> 照 AUDIT.md 第三層做一次深掃。

然後照下面這份清單做，每一項都要**實測**，不能用讀程式碼代替：

1. **沙箱**：試著從策略程式碼裡拿到 `os.environ`、`sys.modules`、檔案系統、網路。
   不要只試 dunder；試單底線別名、格式字串、算出來的屬性名、例外物件、子類別走訪。
2. **跨帳號**：不要看 router 的 filter，直接用兩個真帳號實測每一條路由的讀、寫、刪。
3. **不需要登入的路面**：把每一個公開端點的回應逐欄看過，問「這一欄的來源是誰的資料」。
4. **秘密的去向**：每一個讀 `settings.*` 或 `os.environ` 的地方，追到它會不會進到某個
   回應、某個 log、某個外送的 HTTP 請求（包含「網址由使用者指定」的那種 SSRF 形狀）。
5. **git 歷史**：不只工作區。`git log -p -S` 找曾經進過版控的金鑰。
6. **前端**：build 出來的檔案裡有沒有帶到不該帶的東西。**內建前端的部署現在第一層
   就會做**（`--url` 會抓 `/` 上引用的 `/assets/*`，用同一組 `REPO_FORBIDDEN` 掃），
   所以深掃這一項要問的是它問不到的：把畫面另外放的那一份、以及規則寫不出來的形狀
   （例如一段看起來無害但拼得出金鑰的程式碼）。

找到的每一件事都要：**先重現**（寫一個會失敗的測試）、**再修**、**再把那個測試留下來**。
規則寫得出來的，順手加進 `scripts/audit.py`——深掃找到的東西應該讓第一層變強，
不然下次還要再找一次。

---

## 誤報怎麼處理

**不要放著。** 這個專案已經寫過一次代價：一個會在沒事時響的警報器，久了就沒有人相信它
（`HEALTH_MAX_AGE_SEC` 那一件）。稽查員裡有一份 `PLACEHOLDER_WORDS`，用來認出文件裡
的範例連線字串——被略過的每一筆還是會印出來，不會安靜地消失。

要放行一個新的東西，兩條路，都不是「把檢查關掉」：

- 公開端點 → 加進 `PUBLIC_ON_PURPOSE`，**附理由**。
- 文件範例 → 加進 `PLACEHOLDER_WORDS`。

---

## 稽查員自己壞掉怎麼辦

這是最危險的失效：一個壞掉的稽查員印出來的是「沒有發現」，跟真的沒事長得一模一樣。

已經發生過一次。第一版的掃描會在中途打到 `POST /api/auth/logout-everywhere`——它就在
被列舉的操作清單裡——結果把自己登出了，後面 73 個操作全部變成未登入，每一扇門都是關的，
報告乾乾淨淨。發現它的只有印在旁邊的狀態碼分佈。

所以現在：

- 每一次掃描都印**狀態碼分佈**。
- 帶著登入身分的掃描如果有超過四分之一被擋在門外，那**本身就是一個發現**。
- 稽查員有自己的測試（`backend/tests/test_audit_catches_it.py`），而且那些測試被
  突變測試過——把保護拿掉，對應的測試就會紅。
