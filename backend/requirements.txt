fastapi>=0.115
uvicorn[standard]>=0.37
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.9
pydantic-settings>=2.15
email-validator>=2.2
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 reads bcrypt.__about__, removed in bcrypt>=4.1 -- pin below that
# until passlib cuts a compatible release (or we drop passlib for direct bcrypt).
bcrypt<4.1
python-jose[cryptography]>=3.3
pyotp>=2.9
python-multipart>=0.0.12
httpx>=0.27
cryptography>=43.0
pywebpush>=2.0
yfinance>=0.2.43
numpy>=1.26
pandas>=2.2
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.16

# Optional cloud Postgres driver (needed once DATABASE_URL points at Neon/Supabase/etc.)
psycopg2-binary>=2.9.12

# Optional: only needed if AI_PROVIDER=anthropic is selected (default provider is
# the plain-httpx OpenAI-compatible client, which needs no extra SDK)
# anthropic>=0.39

# Declared, not left to chance: services/market_calendar.py resolves
# Asia/Taipei and America/New_York through zoneinfo, which falls back to this
# package when the host has no system tz database. Windows never has one, and
# python:3.13-slim -- the deploy image -- does not ship one either, so without
# this the container would fail at import and never start. It arrives today
# only as somebody else's transitive dependency, which is not a guarantee.
tzdata>=2026.1
