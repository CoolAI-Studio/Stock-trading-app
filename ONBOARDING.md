# 引導流程 SOP：從「帳號建好了」到「第一則提醒送到手機」

這份文件是**規格**，不是說明書。它定義新使用者建立帳號之後會看到什麼、被問什麼、
系統替他做什麼，以及每一步失敗時要怎麼講。實作照這份做；改流程先改這份。

讀者是**不是工程師的擁有者**。整份文件的判準只有一條：

> 他按了 Deploy、把設定填完、建了帳號。**多久之後，他的手機會響第一次？**
> 中間有沒有任何一步需要他寫程式、開終端機、或去別的地方查一個值？

---

## 一、現況（實測，2026-08-22）

帳號建好、登入之後，他看到的是一個**空的儀表板**：沒有自選股、沒有策略、
沒有通知管道。畫面上沒有任何一句話告訴他下一步該做什麼。

假設他想要最簡單的那件事——「台積電跌到 900 塊叫我」。今天唯一的路是：

1. 到「策略」頁 → 新增策略
2. 從範例挑 `price_alert.py`
3. **在一個程式碼編輯器裡**，把 `self.symbol = "2330.TW"` 和
   `self.buy_below = 950.0` 改成他要的值
4. 存檔 → 啟用
5. 再到「通知」頁設定一個管道（不然前四步等於沒做）

第 3 步就是流程斷掉的地方。CLAUDE.md 對這件事的定位沒有模糊空間：

> **不用寫 Python 就能設定的簡單價格提醒，是核心功能，不是加分項。**

而它今天不存在。範例檔讓改動變成「改三個數字」，但畫面上仍然是一個程式碼編輯器，
而「改三個數字」和「不用寫程式」對這個使用者是兩件不同的事。

**另一條路是 AI**（`POST /api/strategies/generate` 用自然語言產生策略程式碼），
它能用，但它需要一把 AI 金鑰。CLAUDE.md 同樣寫得很清楚：

> **AI 輔助不能是設定流程的必需品。** AI 需要 `AI_API_KEY`，那本身就是一格空白；
> 讓設定依賴它就循環了。

所以現在的狀態是：**要嘛寫 Python，要嘛付錢給 AI。** 兩條路都違反產品自己的定位。

---

## 二、目標流程

> **修正（2026-08-22）**：這一節原本寫的是「引導你設好第一則提醒」。那是誤讀。
> 使用者要的引導是**設定引導**：
>
> > 引導只是引導如何讓 AI 的 API 上線，還有幾個建議資料庫如何讓它連上線，
> > 或是引導用本機的設定。其他有要 AI 的部分，在沒有 AI 導入前功能可以關起來。
> >
> > 骨架：這應該是個分頁的顯示，即時，每個選項作動必須成功。
>
> 所以引導的本體是下面的「設定引導」，而問句式的歡迎畫面只要**基本 Q&A**。

### 1. 設定引導（本體）：`/guide`，三個分頁

| 分頁 | 要接上線的東西 | 「作動必須成功」是什麼 |
| --- | --- | --- |
| 資料庫 | 一個 Postgres 連線字串，或明確選擇本機檔案 | 「重新檢查」問系統現在真的用的是什麼 |
| AI 的 API | 一把他自己的金鑰 | 「測試 AI」真的去問一次，通了才算 |
| 通知 | 至少一個管道 | 「傳一則測試」真的送出去，而且**他要真的收到** |

三個共同的規矩：

1. **狀態是問來的，不是記著的。** 每個分頁上的 ✅／⭕ 來自後端的現況
   （`/api/system/status` 的 `database`、`/api/ai-settings` 的 `configured`、
   通知管道清單），不是畫面上存的一個布林值。「填了」跟「通了」是兩件事。
2. **指示要用他的平台的說法。** `/api/system/status` 會回 `platform.env_where`
   （Render 的 Environment、Fly.io 的 secrets、Heroku 的 Config Vars…）。
   對一個部署在 Fly.io 的人說「去 Render 後台」，比含糊更糟——他會真的去找。
3. **一打開就停在第一個還沒完成的分頁**，不要讓他自己找。

**資料庫的三個選項**（都要完整，因為這是他最可能卡住的地方）：

- **雲端免費 Postgres**：Neon、Supabase。註冊 → 建資料庫 → 複製連線字串 →
  貼進 `DATABASE_URL`（用他平台的說法指路）→ 存檔後自動重啟 → 回來按重新檢查。
- **付費或自架 Postgres**：一樣是貼一個連線字串。這個 app 不在乎是誰家的。
- **本機檔案（SQLite）**：**這是一個正當的選擇，不是沒設定完。** 在自己的機器上
  那個檔案不會消失。app 現在分得出「雲端平台上的檔案」（會被清空，要擋）和
  「本機的檔案」（是選擇，只提醒備份）——因為它認得出自己跑在哪裡。

**AI 沒接上線的時候，需要 AI 的功能要關起來**，不是給一個按了會失敗的按鈕：
策略頁的「用 AI 產生策略」整塊換成一句話加一條路，歡迎畫面的「讓 AI 幫我」
根本不出現。其他功能完全不受影響——這也是 CLAUDE.md 那條規則的另一面。

### 2. 基本 Q&A：`/welcome`

一個問題：**你想先做什麼？**

- **把設定弄完**（→ `/guide`）← 排第一，因為那才是他真正會卡住的地方
- 設一則提醒（現成的範本，不用寫程式）
- 讓 AI 幫我（**只有 AI 已經接上線才出現**）
- 直接進儀表板

沒有策略、沒有自選股、沒有通知管道的帳號打開首頁會先被帶到這裡一次；他自己選擇
離開之後就不再攔他（否則帳號還是空的，會變成一個沒有出口的迴圈）。

---

## 三、產品功能：不用寫程式的提醒範本

（這一節講的是**產品功能**，不是引導本身。它之所以在這份文件裡，是因為寫這份
SOP 的時候實測發現它不存在——而 CLAUDE.md 把它列為核心功能。）

四個方案，難度由低到高。**每一個都是表單，不是程式碼。**

### 方案 1｜到價提醒（最小、最常用）

- **他填**：股票代號、跌破多少通知我、漲過多少通知我（兩個價位可以只填一個）
- **系統做**：加進自選股 ＋ 建立一條到價提醒
- **適合**：只想要「跌到我想買的價位叫我」

這一個是其他三個的地基，也是 CLAUDE.md 指名的核心功能。**第一版一定要有。**

### 方案 2｜每日收盤摘要

- **他填**：幾支股票
- **系統做**：加進自選股 ＋ 一條「收盤後把當日漲跌整理成一則通知」
- **適合**：不想被盤中打擾的人

### 方案 3｜內建規則盯盤（不用寫 Python 的技術面）

- **他填**：股票代號 ＋ 從清單裡選一條規則
- **規則清單（第一版建議）**：
  - 跌破 20 日均線
  - 突破 60 日新高
  - 從最近高點回落 10%
  - RSI 低於 30（可能超賣）
- **系統做**：建立對應的提醒
- **適合**：想要一點訊號，但不想寫程式

現有的 `ma_cross.py`、`rsi_threshold.py` 範例證明這些規則引擎跑得動——缺的只是
「用表單填參數」這一層。

### 方案 4｜TradingView 訊號轉發

- **系統給他**：webhook 網址 ＋ 密碼（都可以直接複製，不用去別的地方拿）
- **他做**：貼進 TradingView 的警報設定，畫面上附一段可以直接複製的訊息範本
- **適合**：已經在 TradingView 上設好警報的人

後端早就做好了，但「密碼」原本拿不到：訊息範本寫著 `<你的 TV_WEBHOOK_SECRET>`，要他去部署平台
翻環境變數。現在每個帳號有一條專屬網址，網址本身就是密碼，所以兩樣都真的可以直接複製（#115）。

---

## 四、通則（每一步都適用，違反就是做錯）

1. **AI 永遠是捷徑，不是必需品。** 拿掉 AI，整條引導要能走完並且做出一則會響的提醒。
2. **app 生得出來的值就給按鈕**，不要叫他去別的地方拿。真的生不出來的（別人家服務
   的連線字串）要老實說，不要假裝。
3. **每一步都可以跳過，除了通知管道。** 跳過通知管道時要明說「現在不會有任何提醒」。
4. **引導只自動出現一次**（判準：這個帳號還沒有任何自選股／策略／通知管道），
   但要能從設定頁隨時再叫出來。
5. **失敗訊息要說「發生了什麼」和「你可以做什麼」。** 「失敗」兩個字不是訊息。
6. **引導做出來的東西是普通資料**，不是隱形設定：自選股就是自選股，提醒就是提醒，
   之後可以照常在各自的頁面編輯或刪除。
7. **引導不下單。** 這是提醒系統。任何方案的結果都只會是「通知」，不會是委託。

---

## 五、實作順序（以及為什麼是這個順序）

| 順序 | 做什麼 | 為什麼是這個位置 |
| --- | --- | --- |
| 1 ✅ | **表單式到價提醒**（後端 ＋ 前端） | 引導的每一條路最後都要落在「一則會響的提醒」上。沒有它，引導只能把人送進程式碼編輯器——也就是今天的樣子 |
| 2 ✅ | 通知管道那一步（含測試按鈕的驗收） | 提醒沒有出口等於沒有提醒。這一步的完成定義是「他真的收到了」 |
| 3 ✅ | 引導骨架（階段 1／2A／4）＋ 方案 1 | 到這裡，一個不寫程式、不用 AI 的人可以從零走到手機響 |
| 4 ✅ | 方案 3（內建規則）與方案 2（收盤摘要） | 方案 3 的引擎本來就有，缺的只是參數表單，跟方案 1 同一批做完。方案 2 當初缺的是「收盤後跑一次」的排程：現在由盯盤迴圈每一輪順便看「是不是某個市場收盤後的時間窗」，窗外一句 SQL 都不送；清單就是自選股（#117） |
| 5 ✅ | 方案 4（TradingView） | 後端早就做好，但缺的不只是「接進引導」：訊息範本要他去部署平台翻出 `TV_WEBHOOK_SECRET`，違反通則 2。做成每個帳號一條專屬網址，網址本身就是密碼（#115） |
| 6 ✅ | 階段 2B（AI 路徑） | **刻意放最後。** 它先做的話，會讓人以為引導已經完成，而那條路需要一把金鑰 |

**目前進度**（2026-09-15）：第 1～6 步全部完成，四個方案都在引導裡。

- 收盤摘要（方案 2，#117）：`/welcome` 的「收盤後給我一則摘要就好」→ 選幾檔股票（走自選股那
  支本來就在驗代號的端點）→ 打開摘要。每個有收盤的市場（台股、美股）收盤後各一則，說每一檔
  當天漲跌多少；儀表板自選股底下有開關、會摘要哪幾檔、上一次送出和沒送成的原因。用報價不用
  日 K（日 K 要等當天結束才放出來），並用成交時間擋掉休市日、用回答時間擋掉盤中的舊報價。
  加密貨幣不收盤，不在摘要裡，畫面上明說。
- TradingView（方案 4，#115）：`/welcome` 的「我已經在 TradingView 設好警報了」→ 一條可以直接
  複製的專屬網址（`/api/webhooks/tradingview/{user_id}.{version}.{mac}`，mac 由
  `TV_WEBHOOK_SECRET` 算出，資料庫裡不存任何新的秘密）＋ 一段不需要密碼的訊息範本。網址本身
  就是密碼，所以畫面上遮著、複製的是完整的；「重新產生」只放在 TradingView 頁。只用
  TradingView 的帳號，不會再被「沒有東西被盯著」的橫幅說成「不會有任何提醒送出」。舊的共用
  密碼網址照舊收件，已經設好的警報不受影響。

- 表單式提醒：`GET /api/strategies/templates`、`POST /api/strategies/from-template`、
  前端 `TemplateAlertForm`。方案 3 的四條規則也在同一批（同一個機制）。
- 通知管道沒有出口時，每一頁上方都會講（`NoChannelBanner`，跟 worker 那個 banner
  同一個位置，因為是同一種失效）。
- 引導本身：`/welcome`（`WelcomePage`）＋ `OnboardingGate`——沒有策略、沒有自選股、
  沒有通知管道的帳號打開首頁會先看到引導。通知那一步先給瀏覽器推播，因為它是唯一
  不用去別的地方拿值的管道。

- AI 路徑（階段 2B）：金鑰設定好之後就在引導裡直接問「用一句話說你要什麼」，
  AI 反問就讓他回答（反問是好結果，不是失敗），**產生的東西一定要按下確認才寫進去**，
  而且失敗一律給得出「改用現成的範本」這條出口——任何一版只要拿掉 AI 之後走不完，
  就是把選填功能做成了必需品。

**還沒做**：沒有了——四個方案都在引導裡（方案 4 見 #115，方案 2 見 #117）。

（這一段原本還寫著「通知管道那一步的完成判準只是『有一個啟用中的管道』」——那已經在 #72
改成以「他真的收到了」為準：設定引導的通知分頁要測試送達才算完成，引導的完成畫面也不會對一個
從沒送成功過的管道說「通知會送到 X」。）

**做法上的建議**：到價提醒不要另外做一套訂閱系統。現有的策略引擎（worker、節流、
通知重送、沙箱）已經在跑，而 `price_alert.py` 範例證明這種規則就是一支很短的策略。
最省事也最不容易出錯的做法是：**表單 → 伺服器端用範本產生那支策略 → 存成一般的策略**。
使用者從頭到尾看不到程式碼，而底下是一條已經被測過幾百次的路。

---

## 六、已經拍板的事（2026-08-22）

1. **第一版的骨架方案：方案 1（到價提醒）＋ 方案 3（內建規則盯盤）。**
   方案 2（收盤摘要）和方案 4（TradingView）不在第一版——方案 2 需要一個
   「收盤後執行一次」的排程，跟現在每 N 秒輪詢的模型不一樣；方案 4 後端已經
   完成，接進引導的成本低，之後補。
2. **方案 3 的規則清單**照第三節那四條：跌破 20 日均線、突破 60 日新高、
   從最近高點回落 10%、RSI 低於 30。
3. **AI 路徑（階段 2B）第一版一起做。** 但實作順序不變，它仍然排在骨架路徑
   之後——理由在第五節：先做它會讓人以為引導已經完成，而那條路需要一把金鑰。
   驗收的判準也不變：**把 AI 拿掉，整條引導要能走完並做出一則會響的提醒。**
