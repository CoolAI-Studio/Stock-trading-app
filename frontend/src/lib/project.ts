/**
 * 這個專案自己的來歷：一個陌生人想要自己一份的時候，該去哪裡。
 *
 * **指向安裝說明，不是指向某一家的部署按鈕。** 第一版這裡是
 * `render.com/deploy?repo=...`，一按就進 Render——那等於替他決定了要用哪一家，
 * 而「不要綁死廠商」是這個專案最早提出的三個需求之一。這個 app 要的是三樣東西，
 * 不是三個品牌：一個能跑 Docker 容器的地方、一個 Postgres、一個放前端的地方，
 * 而且三樣都可以在他自己的電腦上。README 裡那張「要的東西／免費的例子／換成別的
 * 可以嗎」對照表就是這件事的單一事實來源，所以這裡連過去，而不是在 app 裡再抄
 * 一份會漂的清單。
 *
 * **只有一份骨架，散出去的是部署不是拷貝。** 每一份部署都指回同一個 repo，理由
 * 有三個，第三個是關鍵：
 *
 *   一、安全修補傳得下去。各自拷貝之後，每一份都是拷貝那一刻的快照。這個 app
 *       修過策略沙箱逃逸和跨帳號隔離——層層轉發之後，最末端的人拿到的是少了那些
 *       修補的版本，而他不會知道。
 *
 *   二、拷貝模式要 app 知道「我的原始碼在哪」，那會是部署表單上的第八個空格。
 *       這個專案已經為了同一條理由否決過 Prometheus。
 *
 *   三、「拷貝到自己的空間」在 GitHub 上就是 fork，而 fork 要有 GitHub 帳號。
 *       目標使用者是想在手機上收股票提醒的人，不是工程師。
 *
 * 改過這份程式碼的人要指向自己的 repo，就設 `VITE_PROJECT_URL`（前端建置時的環境
 * 變數，Vercel 那類平台上填一格就好），或直接改下面這個常數——他既然已經在改程
 * 式碼，改一行不是負擔；而沒改的人自動跟著上游更新。
 *
 * 指向的是 `docs/index.html`（GitHub Pages），不是 repo 首頁。這個 repo 有五百多
 * 個檔案、其中一百八十幾個是測試，而一個只想在手機上收提醒的人打開它只會迷路。
 * 那一頁把兩件事講完——放自己電腦還是放雲端、雲端要用哪一家——然後才連回原始碼。
 * **同一個 repo 裡的一頁 HTML，不是第二個 repo**：安全修補一次到位，沒有兩份要
 * 同步的東西。
 */
export const PROJECT_URL =
  import.meta.env.VITE_PROJECT_URL ?? 'https://coolai-studio.github.io/Stock-trading-app/'
