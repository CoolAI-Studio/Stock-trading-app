/**
 * 這張圖打開時給多少、往前拉到哪裡就去要更早的。
 *
 * 放在這裡而不是 PriceChart.tsx 裡面，是因為**開真的瀏覽器的那一關讀不到那個
 * 檔案**：它跑在 node 專案下（tsconfig.node.json），而 PriceChart.tsx 是 JSX ＋
 * DOM 型別。把數字抄一份過去就會漂——而漂掉的那一天，測試會用 120 驗一個已經
 * 改成 200 的行為，然後很有信心地綠燈。
 */

/**
 * 打開時畫面上有幾根 K 棒。
 *
 * **不是 fitContent()。** 把全部塞滿正是第一次修這件事時弄壞的地方：可見範圍變
 * 成整段載入的歷史，再配上 fixLeftEdge（它存在的理由是擋住捲進空白），函式庫就
 * 會在使用者往左拖的時候拒絕移動——因為已經顯示的東西左邊真的沒有別的了。十條
 * 測試綠、CI 綠、部署成功，全部沒看到。
 *
 * 改成只顯示最近這一段，歷史留在畫面左外側：第一秒就拉得動，而拉到最舊那一根
 * 就是去要更早的資料的訊號。
 */
export const INITIAL_VISIBLE_BARS = 120

/**
 * 可見範圍的左緣離最舊那一根還有幾根，就先去要下一段。
 *
 * 等到貼齊才要，使用者會先看到自己撞到牆。
 */
export const LOAD_MORE_WITHIN_BARS = 20
